{
  "compilerOptions": {
    "moduleResolution": "node",
    "strict": true,
    "paths": {
      "dat.gui": ["../../node_modules/@types/dat.gui"],
      "three": ["../../node_modules/@types/three"]
      // this means that when you see this in the source code, use this type defination at that address
    }
  },
  "include": ["**/*.ts"], //The include option tells the TypeScript compiler to
  // target all files in all subdirectories within the src directory
  "exclude": ["node_modules"]
  // The exclude option makes sure the TypeScript compiler doesn’t dive into
  // any directories associated with project dependencies (i.e. things we are writing.)
}
